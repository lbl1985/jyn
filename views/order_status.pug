extends ./layout.pug
block headerTitle
    title 订单状态
block otherScripts
    <script src="js/order_status_controller.js"></script>
block body
    h2 订单状态
    #order_status
        table#order_status_table
            colgroup
                col(width="0%")
                col(width="20%")
                col(width="20%")
                col(width="20%")
                col(width="20%")
                col(width="10%")
                col(width="10%")
            thead
                tr
                    th(style="display:none") uuid
                    th 订单名
                    th 地址
                    th 联系人
                    th 状态
                    th 责任人
                    th 更改
            tbody
                each order in orders
                    tr
                        - var assign_to = "";                        
                        td(class=["uuid", "not"]) #{order.uuid}
                        td(class=['td_title']) #{order.title}
                        td(class=['td_ship_add']) #{order.ship_add}
                        td(class=['td_contact_name']) lbl                        
                        td(class=['td_status']) #{order.status.status}
                        td(class=['td_assign_to']) #{order.status.assign_to}
                        td 
                            nav
                                button(type='button' class=['btn', 'btn-info', 'deleteOrder']) 删除
                                button(type='button' class=['btn', 'btn-info', 'editOrder']) 编辑
                    

